# GENERATED by scripts/blocks.py from blocks.json (blocks: web, api, worker, postgres).
# Do not edit by hand: change the fragments it includes, then run
#   python3 scripts/blocks.py sync
# Each block's fragments live next to its code; docs/BLOCKS.md explains them.
#
# LOCAL DEVELOPMENT ONLY. Production is compose.deploy.yml.
#
# Unlike production, this publishes ports (there is no shared Caddy on a
# laptop) and mounts source for hot reload. Ports are overridable in .env
# (DEV_WEB_PORT / DEV_API_PORT / DEV_DB_PORT).
#
#   docker compose up --build

include:
  - path: backend/compose/postgres.dev.yml
    project_directory: .
  - path:
      - backend/compose/api.dev.yml
      - backend/compose/api.dev.with-postgres.yml
    project_directory: .
  - path:
      - backend/compose/worker.dev.yml
      - backend/compose/worker.dev.with-postgres.yml
    project_directory: .
  - path:
      - frontend/compose/web.dev.yml
      - frontend/compose/web.dev.with-api.yml
    project_directory: .
